# Скопируй в .env для локального запуска (node --env-file=.env scripts/generate-digest.mjs)
# В GitHub Actions эти же имена задаются как Repository Secrets — см. README.

ANTHROPIC_API_KEY=sk-ant-...
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_SERVICE_KEY=eyJ...   # service_role key из Project Settings -> API (НЕ anon key!)
